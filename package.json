{
  "name": "islam-menjawab",
  "private": true,
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "scripts": {
    "deploy:pages": "wrangler pages deploy public --project-name=islam-menjawab",
    "deploy:worker": "cd cf-worker && wrangler deploy",
    "dev": "wrangler pages dev public --wrangler-config=cf-worker/wrangler.toml"
  }
}
